import 'package:bacteriofago/alarms.dart';
import 'package:flutter/material.dart';

class MedicineNotifications extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Column(
      children: [
        Alarms()
      ],
    );
  }
}
